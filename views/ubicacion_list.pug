extends layout

block content
  h1= title
  
  ul
  each ubicacion in ubicacion_list
    li
      a(href=ubicacion.url) #{ubicacion.ciudad} 
      | (#{ubicacion.pais} - #{ubicacion.continente})

  else
    li No hay ubicaiones.